.org 0x0

startup:
    sid
    mov 0, RA
    mov 1, RB
    mov 0x42, RC
    cid
loop:
    cmp RA, RC
    jz  end
    goto loop
end:
    hlt


.org 0x1f00
    add RA, RB
    mov 0x01, [0x4000]
    iret
    